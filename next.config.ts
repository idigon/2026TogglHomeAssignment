import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // The dev badge sits in the bottom-left corner, on top of the account avatar.
  devIndicators: false,
};

export default nextConfig;
